package org.mentorat.exo.collections;

import java.security.SecureRandom;
import java.util.List;
import java.util.Random;
import java.util.random.RandomGenerator;

public class ExoList {


    //1. Initialiser une Liste
    /*
           Dans cette méthode initialiser unje liste de 200 entiers avec uniquement les valeurs pair compris entre 0 et 400 (inclusif)
           Tips : Modulo => 20 % 2 = 10 // x % 2 == 0 ? even : odd
                */
    public List<Integer> initializeEvenInteger()
    {
        return List.of();
    }

    //2 Faire la somme de la liste ci dessus un nombre aléatoirement de fois et faire le total de ces sommes.
    // Tips : généré un nombre aléatoire avec la classe RandomGenerator, declaration possible :
    // RandomGenerator rand = new SecureRandom();

    public Long sommeOFRandomInitializeList()
    {
        return 0L;
    }

}
